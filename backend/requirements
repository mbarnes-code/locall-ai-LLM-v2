# Django Framework (LTS version)
Django>=3.2,<4.0

# Django REST Framework (for APIs)
djangorestframework>=3.12.4,<3.14

# Gunicorn (Production WSGI server)
gunicorn>=20.1.0,<21.0

# CORS Headers (Allow frontend to access backend APIs)
django-cors-headers>=3.13.0,<4.0

# Database Adapter (PostgreSQL recommended, SQLite alternative)
psycopg2-binary>=2.9.6,<3.0  # PostgreSQL
# sqlite3 is built into Python, so no package needed if using SQLite

# Environment Variables Handling (For `.env` support)
django-environ>=0.10.0,<1.0  # Secure way to manage secrets & DB connections
